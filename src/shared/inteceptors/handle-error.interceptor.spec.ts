import { HandleErrorInterceptor } from './handle-error.interceptor';

describe('HandleErrorInterceptor', () => {
  it('should be defined', () => {
    expect(new HandleErrorInterceptor()).toBeDefined();
  });
});
